# dns-zone-transfer
Script em Bash que identifica os nameservers autoritativos de um domínio e tenta realizar transferência de zona DNS (AXFR) utilizando o comando host.  O script automatiza a tentativa de transferência contra cada servidor DNS encontrado.
